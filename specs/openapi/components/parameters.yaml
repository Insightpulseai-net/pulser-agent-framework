# GENERATED FILE - DO NOT EDIT MANUALLY
# Source: specs/openapi/components/parameters.yaml
# Generated: 2026-01-01T00:00:00Z
# Regenerate: make generate-spec
components:
  parameters:
    PageSize:
      name: page_size
      in: query
      description: Number of items per page
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    PageToken:
      name: page_token
      in: query
      description: Token for pagination
      schema:
        type: string

    Filter:
      name: filter
      in: query
      description: Filter expression
      schema:
        type: string

    DocumentId:
      name: documentId
      in: path
      required: true
      description: Unique document identifier
      schema:
        type: string
        format: uuid

    ArtifactId:
      name: artifactId
      in: path
      required: true
      description: Unique artifact identifier
      schema:
        type: string
        format: uuid

    AgentId:
      name: agentId
      in: path
      required: true
      description: Unique agent identifier
      schema:
        type: string

    SortBy:
      name: sort_by
      in: query
      description: Field to sort by
      schema:
        type: string
        enum: [created_at, updated_at, name]
        default: created_at

    SortOrder:
      name: sort_order
      in: query
      description: Sort order
      schema:
        type: string
        enum: [asc, desc]
        default: desc
