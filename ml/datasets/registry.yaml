# GENERATED FILE - DO NOT EDIT MANUALLY
# Source: docs-to-code-pipeline Model Factory
# Generated: 2026-01-01T00:00:00Z
# Regenerate: Managed by repository template
---
# Dataset Registry
# Tracks dataset versions, hashes, and provenance for reproducible training

registry_version: "1.0"
storage_backend: "local"  # local, s3, gcs, azure

datasets:
  # Example dataset entry (template)
  # sft_v1:
  #   version: "1.0.0"
  #   description: "Supervised fine-tuning dataset v1"
  #   created_at: "2026-01-01T00:00:00Z"
  #   source: "docs2code"
  #   format: "jsonl"
  #   path: "datasets/sft_v1/train.jsonl"
  #   content_sha256: "..."
  #   row_count: 10000
  #   splits:
  #     train: 8000
  #     validation: 1000
  #     test: 1000
  #   schema:
  #     - name: instruction
  #       type: string
  #       required: true
  #     - name: input
  #       type: string
  #       required: false
  #     - name: output
  #       type: string
  #       required: true
  #   provenance:
  #     source_artifacts:
  #       - artifact_id: "..."
  #     builder: "builders/docs_to_jsonl.py"
  #     config_hash: "..."

# Dataset validation rules
validation:
  require_hash: true
  require_provenance: true
  max_age_days: 365  # Warn if dataset older than this

# Default paths
paths:
  raw: "ml/datasets/raw/"
  processed: "ml/datasets/processed/"
  cache: "ml/datasets/.cache/"
